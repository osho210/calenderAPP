import React from "react";

//containerの値をpropsで渡しているからpropsの値がある
perse
const Counter = ({ count, increment, decrement }) => (
    <>
        <div>{count}</div>
        <button onClick={() => increment(1)}>+</button>
        <button onClick={() => decrement(1)}>-</button> */}
    </>

)
export default Counter;
